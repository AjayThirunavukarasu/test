plugins {
    id 'java'
    id 'maven-publish'
}

group = 'com.example'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // Your project dependencies go here
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java

            // Optionally, you can customize the publication metadata
            groupId = 'com.example'
            artifactId = 'your-artifact-id'
            version = '1.0.0'
        }
    }
    repositories {
        maven {
            // Specify the URL where you want to deploy the artifacts
            url = uri('file://path/to/local/repo') // or use a remote repository URL
        }
    }
}
